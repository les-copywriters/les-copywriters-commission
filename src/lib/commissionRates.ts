/**
 * Commission rates applied to the HT (ex-tax) amount.
 * Update these values here only — all calculations across the app
 * reference this single source of truth.
 */
export const CLOSER_RATE = 0.088;        // 8.8%
export const SETTER_RATE = 0.01;         // 1.0%
export const PIF_BONUS_PER_SALE = 50;    // €50 per PIF sale (monthly closer bonus)
